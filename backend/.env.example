# Firebase Admin credentials (service account JSON path)
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# Analytics / BigQuery dataset IDs
BQ_DATASET=englishbrain_analytics

# OpenAPI mock base URL (override for local testing)
API_BASE_URL=http://localhost:3001

# Brain Burst scheduling interval (sessions)
BRAIN_BURST_INTERVAL=5
